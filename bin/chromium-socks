#!/bin/sh

SOCKET_LOCATION="/tmp/socks.%C"

ssh -fCND 1080 -MS "$SOCKET_LOCATION" "$@"
killall chromium
chromium --proxy-server="socks5://localhost:1080" --host-resolver-rules="MAP * 0.0.0.0, EXCLUDE localhost"
ssh -S "$SOCKET_LOCATION" -O exit
